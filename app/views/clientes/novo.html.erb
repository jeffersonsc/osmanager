<h1> Cadastrar Cliente </h1>

<div class="form">	
	<%= form_for(@cliente_novo) do |f| %>
		<% if @cliente_novo.errors.any? %>
	    <ul>
	      <% @cliente_novo.errors.full_messages.each do |msg| %>
	      <li><%= msg %></li>
	      <% end %>
	    </ul>
	 <% end %>
		<%= render(:partial => "form_clientes" , :locals => { :f => f} ) %>
	<% end %>

</div>